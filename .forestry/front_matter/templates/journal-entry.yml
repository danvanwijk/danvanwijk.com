---
label: Journal Entry
hide_body: false
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- type: datetime
  name: date
  label: Publish Date
  config:
    date_format: YYYY-MM-DD
    export_format: YYYY-MM-DD
    time_format: ''
    required: true
  default: now
- name: feature
  type: file
  config:
    maxSize: 64
  label: Image
pages:
- content/journal/2020/12/30.md
